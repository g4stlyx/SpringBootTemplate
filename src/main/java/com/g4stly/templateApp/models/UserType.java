package  com.g4stly.templateApp.models;

/**
 * Enum representing the different types of users in the system
 */
public enum UserType {
    USER,    // User - there might be different types of users too based on the app structure. (like students, coaches/clients etc.)
    ADMIN    // Admin - system administrators with different levels (0, 1, 2)
}
